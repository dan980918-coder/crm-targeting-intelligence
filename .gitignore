# Raw / external source data (CC BY-NC 4.0, not for redistribution)
data/raw/
data/external/

# Databases
*.duckdb
*.db

# Environment / secrets
.env
.env.*
!.env.example

# Trained model artifacts (root-level only — src/models/, reports/model_cards/ are source/docs, not artifacts)
/models/
/artifacts/

# 고객 단위 원본 산출물 (수백만 행, 재실행으로 재생성 가능 — reports/의 다른
# 집계 CSV/md와 달리 git에 올릴 크기가 아님)
reports/phase8_experiment_assignment.csv

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# OS / editor
.DS_Store
.vscode/
.idea/
