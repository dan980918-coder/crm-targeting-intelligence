# CLAUDE.md

## 1. 프로젝트 개요

### 프로젝트명

**Customer Lifecycle & CRM Targeting Intelligence**

### 프로젝트 목적

실제 이커머스 고객의 검색, 방문, 장바구니, 구매 행동을 통합하여 고객 라이프사이클을 분석하고, 구매 비활성 위험 고객과 향후 구매 가능성이 높은 고객을 식별한다.

최종적으로 제한된 CRM 접촉 자원 안에서 다음 고객을 우선 선정할 수 있는 분석 시스템을 구축한다.

* 재활성화가 필요한 고객
* 구매 유도가 필요한 고객
* 장바구니 이탈 고객
* 반복구매 가능성이 높은 고객
* 접촉 우선순위가 낮은 고객

이 프로젝트는 실제 CRM 캠페인을 집행해 이탈률이나 매출을 직접 개선한 프로젝트가 아니다.

프로젝트에서 검증할 수 있는 핵심 성과는 다음과 같다.

> 단순 규칙이나 무작위 타기팅보다 구매 비활성 위험 고객과 향후 구매 고객을 더 효율적으로 선별할 수 있는가?

---

## 2. 사용 데이터

### 데이터셋

**Synerise RecSys Challenge 2025 원본 데이터**

실제 온라인 리테일 서비스에서 발생한 익명화 고객 행동 로그를 사용한다.

주요 이벤트는 다음과 같다.

* `page_visit`
* `search_query`
* `add_to_cart`
* `remove_from_cart`
* `product_buy`
* `product_properties`

### 데이터 사용 원칙

* 공식 Synerise 배포본만 사용한다.
* Kaggle이나 비공식 복제본을 우선 사용하지 않는다.
* 원본 데이터는 GitHub에 업로드하지 않는다.
* 데이터 출처와 라이선스를 README에 표시한다.
* 공개 대시보드에는 원본 고객별 행동 로그를 포함하지 않는다.
* 공개 결과물에는 집계 데이터, 모델 평가 결과, 합성 샘플만 포함한다.
* 고객 ID는 외부에 그대로 노출하지 않는다.
* 원본 데이터를 복원할 수 있는 세부 테이블을 공개하지 않는다.

### 중요한 검증 조건

데이터가 2025년에 공개됐다는 사실과 실제 행동이 2025년에 발생했다는 사실은 다르다.

첫 단계에서 반드시 모든 이벤트 파일의 다음 값을 확인한다.

* 최소 타임스탬프
* 최대 타임스탬프
* 전체 관찰 기간
* 최근 3년 조건 충족 여부

기준일은 2026년 8월 5일이다.

사용자가 요청한 최근 3년 기준은 원칙적으로 다음과 같다.

```text
2023-08-05 이후 발생한 행동 데이터
```

데이터가 이 조건을 충족하지 못하면 분석을 계속하지 않고 결과를 보고한다.

**[2026-08-05 정책 갱신]** Phase 1 8.3 타임스탬프 검증 결과, 5개 이벤트 테이블
전체가 2022-06-23~2022-12-08(167일) 범위에 있고 2023-08-05 이후 데이터가
0.0000%로 확인됨에 따라, 사용자 승인 하에 이 하드 컷오프 중단 규칙을 다음과
같이 조정한다.

* 데이터가 "최근 3년" 기준을 충족하지 못한다는 사실 자체만으로 Phase 1을 중단하지 않는다.
* 대신 프로젝트 전체 문서(README, 대시보드, LLM 리포트 등)에 데이터 관측 기간을 투명하게 명시한다:
  "본 프로젝트는 2022년 6~12월 스냅샷 데이터를 사용하며, 이는 최신 고객 행동이 아닌 특정 시점의
  이커머스 행동 패턴 분석임을 명시한다."
* 결정 근거와 트레이드오프는 `docs/methodology.md`, 데이터 최신성 관련 한계는 `docs/limitations.md`에 기록한다.
* 이 갱신은 최신성 기준만 대상으로 하며, CLAUDE.md 10번의 다른 중단 조건(연결성 부족, 매칭률 과소 등)은 그대로 유효하다.

---

## 3. 기술 스택

### 필수 기술

* Python 3.12
* DuckDB
* SQL
* Parquet
* Pandas 또는 Polars
* scikit-learn
* pytest
* Streamlit
* Plotly
* Pydantic
* Git
* GitHub

### 선택 기술

필요한 경우에만 사용한다.

* LightGBM
* XGBoost
* CatBoost
* SHAP
* MLflow
* OpenAI API 또는 다른 LLM API
* dbt-core

### 사용하지 않는 기술

* R
* CRAN
* RDS
* RDA
* pyreadr
* Jupyter Notebook에만 의존하는 구조
* 출처가 불분명한 Kaggle 데이터
* 검증되지 않은 합성 데이터
* 처음부터 복잡한 딥러닝 모델

모든 핵심 분석은 SQL과 Python으로 재현 가능해야 한다.

---

## 4. 프로젝트의 핵심 질문

프로젝트는 다음 질문을 중심으로 구성한다.

### 고객 이해

1. 고객은 탐색에서 구매까지 어떤 행동 경로를 보이는가?
2. 어떤 고객이 반복구매자로 이어지는가?
3. 첫 관측 구매 이후 재구매율은 어떻게 변하는가?
4. 구매가 감소하기 전에 어떤 행동 변화가 나타나는가?
5. 장바구니 추가와 제거 행동은 이후 구매와 어떤 관계가 있는가?
6. 고객은 어떤 라이프사이클 상태로 구분할 수 있는가?

### CRM 타기팅

1. 향후 일정 기간 구매하지 않을 가능성이 높은 고객은 누구인가?
2. 향후 일정 기간 구매할 가능성이 높은 고객은 누구인가?
3. 장바구니 행동은 있지만 구매하지 않을 고객은 누구인가?
4. 전체 고객 중 일부만 접촉할 때 누구를 선택해야 하는가?
5. 모델 기반 타기팅은 단순 최근성 규칙보다 효율적인가?

### 실무 활용

1. CRM 담당자가 대시보드에서 접촉 비율을 선택할 수 있는가?
2. 고객 세그먼트별로 다른 CRM 목적을 제안할 수 있는가?
3. SQL로 계산한 결과를 LLM이 정확하게 설명할 수 있는가?
4. 사실, 모델 예측, LLM 제안을 명확하게 구분할 수 있는가?

---

## 5. 프로젝트에서 주장할 수 있는 것과 없는 것

### 주장할 수 있는 것

* 고객행동 기반 CRM 데이터마트를 구축했다.
* 고객 퍼널, 코호트, 리텐션, 구매주기를 분석했다.
* 고객 라이프사이클과 행동 세그먼트를 설계했다.
* 구매 비활성 위험과 향후 구매 가능성을 예측했다.
* 단순 규칙 대비 고객 선정 효율을 비교했다.
* 동일한 접촉 인원에서 위험 고객 포착률을 높였다.
* 동일한 포착률을 달성하는 데 필요한 접촉 인원을 줄였다.
* 분석 결과를 대시보드와 LLM 리포트로 연결했다.

### 주장하면 안 되는 것

* 실제 이탈률을 개선했다.
* 실제 매출을 증가시켰다.
* CRM 캠페인 전환율을 향상했다.
* 쿠폰 효과를 검증했다.
* 실제 고객생애가치를 계산했다.
* 실제 캠페인 ROI를 개선했다.
* 기업이 모델을 운영에 적용했다.
* 모델이 원인적으로 고객 행동을 변화시켰다.

성과 표현은 반드시 다음 형태를 따른다.

```text
실제 비즈니스 KPI 개선
X

과거 행동 데이터를 이용한 대상 선정 효율 개선
O
```

---

## 6. 전체 진행 단계

프로젝트를 한 번에 진행하지 않는다.

각 단계가 끝날 때 결과를 보고하고, 사용자의 검토 없이 다음 단계의 핵심 기준을 임의로 확정하지 않는다.

### Phase 1. 데이터 확보 및 적합성 검증

### Phase 2. 고객행동 데이터마트 설계

### Phase 3. 퍼널·코호트·리텐션 분석

### Phase 4. 라이프사이클·세그먼트 설계

### Phase 5. 기준일별 고객 스냅샷 구축

### Phase 6. 구매 비활성·구매성향 모델링

### Phase 7. CRM 타기팅 시뮬레이션

### Phase 8. Streamlit 대시보드

### Phase 9. LLM CRM 리포트

### Phase 10. GitHub 포트폴리오 문서화

---

# Phase 1. 데이터 확보 및 적합성 검증

## 7. Phase 1의 목적

분석 주제, 이탈 기준, 코호트 기준, 모델을 먼저 정하지 않는다.

원본 데이터를 직접 확인하여 다음을 판단한다.

* 실제 날짜 범위
* 데이터 규모
* 고객 추적 가능성
* 이벤트 간 연결 가능성
* 코호트 분석 가능성
* 퍼널 분석 가능성
* 반복구매 분석 가능성
* 라이프사이클 분석 가능성
* 모델 학습 가능성
* 대시보드 배포 가능성

## 8. Phase 1에서 수행할 작업

### 8.1 데이터 다운로드

공식 Synerise 데이터 페이지에서 원본 데이터를 다운로드한다.

원본 데이터와 전처리된 대회용 데이터가 모두 있을 경우 원본 데이터의 구조를 우선 확인한다.

다운로드 후 다음을 기록한다.

* 파일명
* 파일 크기
* 압축 파일 크기
* 압축 해제 후 크기
* 파일별 형식
* 체크섬
* 다운로드 날짜
* 데이터 라이선스
* 공식 출처

### 8.2 파일 구조 검사

모든 파일에 대해 다음을 출력한다.

* 파일명
* 행 수
* 열 수
* 컬럼명
* 데이터 타입
* 파일 크기
* 첫 5행
* 마지막 5행
* 결측값 수
* 고유값 수
* 예상 PK 후보
* 중복 행 수

Parquet 파일은 전체를 Pandas로 메모리에 올리지 않는다.

DuckDB 또는 Polars의 lazy scan을 우선 사용한다.

### 8.3 타임스탬프 검증

모든 이벤트 테이블에 대해 다음을 계산한다.

```sql
SELECT
    MIN(timestamp) AS min_timestamp,
    MAX(timestamp) AS max_timestamp,
    COUNT(*) AS row_count,
    COUNT(DISTINCT client_id) AS unique_clients
FROM event_table;
```

모든 결과를 하나의 표로 저장한다.

예시:

| event_type       | min_timestamp | max_timestamp | rows | clients |
| ---------------- | ------------- | ------------- | ---: | ------: |
| product_buy      |               |               |      |         |
| add_to_cart      |               |               |      |         |
| remove_from_cart |               |               |      |         |
| page_visit       |               |               |      |         |
| search_query     |               |               |      |         |

검증 결과가 최근 3년 조건에 맞지 않으면 Phase 1에서 중단한다.

**[2026-08-05 정책 갱신]** 위 2번 섹션 "중요한 검증 조건"의 정책 갱신에 따라,
최신성 기준 미충족은 더 이상 단독 중단 사유가 아니다. 대신 관측 기간을
`docs/methodology.md` / `docs/limitations.md`에 명시하고 Phase 1을 계속 진행한다.

### 8.4 고객 ID 연결성 검사

다음 고객 수를 계산한다.

* 전체 고유 고객
* 구매 고객
* 장바구니 고객
* 검색 고객
* 페이지 방문 고객
* 구매와 장바구니가 모두 있는 고객
* 구매와 검색이 모두 있는 고객
* 다섯 이벤트가 모두 있는 고객
* 하나의 이벤트만 가진 고객

이벤트 간 고객 ID 교집합과 합집합을 확인한다.

고객별 이벤트 타입 수 분포도 계산한다.

### 8.5 상품 ID 연결성 검사

다음을 확인한다.

* 구매 상품 수
* 장바구니 상품 수
* 상품 속성 테이블 상품 수
* 구매 상품과 상품 속성 테이블 매칭률
* 장바구니 상품과 상품 속성 테이블 매칭률
* 카테고리 결측률
* 가격구간 결측률
* 중복 상품 속성
* 한 상품에 여러 속성값이 존재하는지

가격은 실제 금액이 아니라 구간 또는 순위형 값일 수 있으므로 실제 매출로 해석하지 않는다.

### 8.6 이벤트 품질 검사

다음을 확인한다.

* 완전 중복 행
* 동일 고객·동일 시각·동일 상품·동일 이벤트 중복
* 타임스탬프 파싱 오류
* 고객 ID 결측
* 상품 ID 결측
* 비정상적으로 많은 이벤트를 가진 고객
* 비정상적으로 짧은 시간에 반복된 이벤트
* 고객별 첫 행동과 마지막 행동
* 고객별 관찰일 수
* 이벤트가 집중된 특정 날짜
* 로그 수집 중단 또는 급증 구간

### 8.7 구매행동 검사

다음을 계산한다.

* 구매 고객 수
* 고객당 구매 이벤트 수
* 고객당 고유 구매일 수
* 고객당 고유 구매 상품 수
* 고객당 구매 카테고리 수
* 고객별 첫 관측 구매일
* 고객별 마지막 관측 구매일
* 고객별 구매 간격
* 구매 1회 고객 비율
* 구매 2회 이상 고객 비율
* 구매 3회 이상 고객 비율
* 반복구매가 가능한 고객 수
* 관찰 종료 직전 구매 고객 비율

구매 간격은 다음을 확인한다.

* 평균
* 중앙값
* 25·75 분위수
* 90·95 분위수
* 고객별 평균 구매간격
* 고객별 구매간격 변동성

### 8.8 장바구니 행동 검사

다음을 계산한다.

* 장바구니 추가 고객 수
* 장바구니 제거 고객 수
* 추가와 제거를 모두 한 고객 수
* 고객별 추가 횟수
* 고객별 제거 횟수
* 상품별 추가 횟수
* 상품별 제거 횟수
* 추가 후 동일 상품 구매 가능성
* 추가 후 구매까지 걸린 시간
* 제거 후 구매한 사례
* 장바구니 이벤트와 구매 이벤트의 시간 연결 가능성

세션 ID가 없다면 임의로 세션을 확정하지 않는다.

세션화가 필요할 경우 다음 후보만 검토한다.

* 30분 비활동 기준
* 60분 비활동 기준
* 일 단위 고객 행동

실제 분포를 확인한 뒤 결정한다.

### 8.9 페이지 방문과 검색 검사

다음을 확인한다.

* 고객별 페이지 방문 횟수
* 고객별 검색 횟수
* 활동일 수
* 구매 전 방문·검색량
* 비구매 고객의 방문·검색량
* 방문 URL과 상품 ID의 연결 가능성
* 검색어 원문의 존재 여부
* 검색어가 임베딩 또는 양자화 값인지
* 해석 가능한 텍스트 필드 존재 여부

상품 페이지와 특정 상품 구매가 직접 연결되지 않으면 상품 단위 퍼널을 만들지 않는다.

고객 단위 탐색 퍼널만 검토한다.

### 8.10 관찰기간과 검열 문제

다음을 확인한다.

* 고객별 첫 관측일
* 고객별 마지막 관측일
* 고객별 관찰기간
* 데이터 시작 시점에 이미 활동 중인 고객
* 데이터 종료 시점 직전 처음 등장한 고객
* 첫 관측 구매를 실제 첫 구매로 볼 수 없는 문제
* 마지막 관측 이후 행동을 알 수 없는 문제

다음 표현을 사용한다.

```text
신규 고객
X

첫 관측 고객
O
```

```text
첫 구매
X

첫 관측 구매
O
```

```text
진정한 고객생애가치
X

6개월 관찰가치 또는 행동가치
O
```

---

## 9. Phase 1 산출물

Phase 1이 끝나면 다음 파일을 생성한다.

```text
reports/
├── phase1_data_profile.md
├── phase1_feasibility_report.md
├── phase1_data_dictionary.csv
├── phase1_event_summary.csv
├── phase1_timestamp_summary.csv
├── phase1_customer_overlap.csv
├── phase1_product_match.csv
├── phase1_data_quality.csv
└── phase1_recommendation.md
```

### `phase1_data_profile.md`

다음을 포함한다.

* 데이터 출처
* 파일 목록
* 데이터 규모
* 컬럼 설명
* 타임스탬프 범위
* 고객 수
* 상품 수
* 이벤트 수
* 결측과 중복
* 고객별 관찰기간
* 구매 분포
* 장바구니 분포
* 이벤트 간 연결성

### `phase1_feasibility_report.md`

다음 분석의 가능 여부를 표로 평가한다.

| 분석          | 가능 | 제한적 가능 | 불가능 | 근거 |
| ----------- | -: | -----: | --: | -- |
| 고객 단위 퍼널    |    |        |     |    |
| 상품 단위 퍼널    |    |        |     |    |
| 구매 코호트      |    |        |     |    |
| 가입 코호트      |    |        |     |    |
| 구매자 리텐션     |    |        |     |    |
| 장기 이탈       |    |        |     |    |
| 14일 구매 비활성  |    |        |     |    |
| 장바구니 이탈     |    |        |     |    |
| 구매성향 예측     |    |        |     |    |
| 카테고리 구매 예측  |    |        |     |    |
| 실제 CLV      |    |        |     |    |
| 캠페인 효과      |    |        |     |    |
| 매출 개선       |    |        |     |    |
| LLM 상품 카피   |    |        |     |    |
| LLM CRM 리포트 |    |        |     |    |

### `phase1_recommendation.md`

데이터 확인 후 가능한 프로젝트 주제 2~3개를 제안한다.

각 주제에는 다음을 포함한다.

* 해결하려는 문제
* 필요한 데이터
* 가능한 KPI
* 가능한 모델
* 대시보드 구성
* LLM 활용 방식
* 장점
* 한계
* 면접에서 받을 가능성이 높은 질문

Phase 1에서는 주제를 임의로 최종 확정하지 않는다.

---

## 10. Phase 1 중단 조건

다음 중 하나라도 발생하면 분석을 계속하지 않고 보고한다.

* 데이터가 공식 배포본인지 확인할 수 없음
* 이용 조건을 확인할 수 없음
* ~~타임스탬프가 최근성 기준을 크게 벗어남~~ → **[2026-08-05 정책 갱신]** 단독 중단 사유에서 제외. 대신 `docs/methodology.md`에 기록된 정책에 따라 관측 기간을 투명하게 명시하고 진행한다. (근거: 2번 섹션 "중요한 검증 조건" 갱신 내역 참조)
* 고객 ID가 이벤트 간 연결되지 않음
* 구매 반복 고객이 너무 적음
* 상품 테이블 매칭률이 지나치게 낮음
* 주요 컬럼 설명이 공식 문서와 다름
* 파일이 손상됐거나 일부 누락됨
* 코호트·리텐션·타기팅 분석이 구조적으로 불가능함

Phase 1 완료 후 사용자 검토 없이 Phase 2로 넘어가지 않는다.

---

# Phase 2. 고객행동 데이터마트

## 11. 데이터마트 기본 구조

Phase 1 통과 후 다음 구조를 만든다.

```text
data/
├── raw/
├── external/
├── interim/
├── processed/
└── dashboard/

sql/
├── staging/
├── intermediate/
├── marts/
├── features/
├── labels/
└── tests/
```

### Staging 테이블

```text
stg_product_buy
stg_add_to_cart
stg_remove_from_cart
stg_page_visit
stg_search_query
stg_product_properties
```

### Intermediate 테이블

```text
int_customer_daily_activity
int_customer_purchase_history
int_customer_purchase_gap
int_customer_cart_behavior
int_customer_category_behavior
int_customer_observation_period
```

### Mart 테이블

```text
mart_customer_360
mart_customer_daily
mart_customer_weekly
mart_customer_cohort
mart_customer_retention
mart_customer_lifecycle
mart_customer_segment
mart_customer_snapshot
mart_churn_target
mart_purchase_propensity
mart_targeting_simulation
```

---

## 12. 테이블 설계 원칙

각 테이블의 다음 내용을 문서화한다.

* 목적
* Grain
* Primary key
* Foreign key
* 컬럼 정의
* 생성 SQL
* 갱신 방식
* 품질 테스트
* 입력 테이블
* 출력 테이블

예시:

```text
Table: mart_customer_snapshot

Grain:
client_id × snapshot_date

Feature Window:
snapshot_date 이전 28일

Label Window:
snapshot_date 이후 14일

Primary Key:
client_id, snapshot_date
```

미래 데이터가 Feature에 섞이지 않도록 한다.

---

# Phase 3. 고객분석

## 13. 퍼널 분석

기본 퍼널 후보는 다음과 같다.

```text
탐색
→ 장바구니 추가
→ 장바구니 유지 또는 제거
→ 구매
```

분석 항목:

* 고객 단위 단계 도달률
* 단계별 전환율
* 단계별 이탈률
* 장바구니 추가 후 구매율
* 검색 행동 후 구매율
* 반복 구매자와 1회 구매자의 퍼널 차이
* 카테고리별 퍼널 차이
* 활동량 구간별 퍼널 차이

페이지 방문과 상품을 연결할 수 없으면 고객 단위 퍼널로 제한한다.

## 14. 코호트 분석

가입일이 없으므로 다음을 사용한다.

```text
첫 관측 구매 주차 코호트
```

분석 항목:

* 7일 재구매율
* 14일 재구매율
* 28일 재구매율
* 주차별 구매자 리텐션
* 코호트별 평균 구매일 수
* 코호트별 반복구매 횟수
* 코호트별 카테고리 다양성

## 15. 리텐션 분석

다음을 구분한다.

* 활동 리텐션
* 구매 리텐션
* 반복구매율
* 구매주기 유지
* 구매 감소
* 비활성 후 복귀

이탈이라는 용어보다 다음 표현을 우선 검토한다.

```text
purchase inactivity
구매 비활성
구매활동 중단 위험
```

## 16. 라이프사이클 후보

실제 분포 확인 후 다음 상태를 검토한다.

* 탐색 고객
* 장바구니 고객
* 첫 관측 구매 고객
* 반복구매 고객
* 활성 구매 고객
* 구매 감소 고객
* 구매 비활성 위험 고객
* 비활성 고객
* 복귀 고객

기준은 임의로 결정하지 않는다.

고객별 구매간격과 행동 분포를 근거로 여러 기준을 비교한다.

---

# Phase 4. 고객 세그먼트

## 17. 세그먼트 설계 원칙

규칙 기반 세그먼트를 먼저 만든다.

군집분석은 규칙 기반 결과를 보완하는 탐색 용도로만 사용한다.

세그먼트 후보:

* 구매 직전 탐색형
* 장바구니 이탈형
* 첫 관측 구매 고관여형
* 안정적 반복구매형
* 반복구매 감소형
* 구매 비활성형
* 복귀형
* 저관여 탐색형

각 세그먼트에 대해 다음을 작성한다.

* 정의
* 고객 수
* 주요 행동
* 구매율
* 구매주기
* 위험 요인
* CRM 목적
* 추천 액션
* 접촉 우선순위
* 과접촉 위험

---

# Phase 5. 모델링

## 18. 모델 A: 구매 비활성 위험

과거 구매 이력이 있는 고객을 대상으로 한다.

목표 예시:

```text
기준일 이후 14일 동안 구매 없음
```

14일 기준은 공식 과제와 비교 가능한 기준이지만, 실제 구매간격 분포와 함께 28일 기준도 비교한다.

Feature 후보:

* 최근 구매 후 경과일
* 최근 7일 구매 수
* 최근 14일 구매 수
* 최근 28일 구매 수
* 평균 구매간격
* 최근 구매간격 변화
* 최근 검색 수
* 최근 방문 수
* 장바구니 추가 수
* 장바구니 제거 수
* 활동일 수
* 카테고리 다양성
* 최근 행동 감소율

## 19. 모델 B: 향후 구매 가능성

활동 고객이 기준일 이후 14일 내 구매할 가능성을 예측한다.

## 20. 기준선

반드시 다음을 비교한다.

* 무작위
* 전체 평균
* 최근 구매일 규칙
* 구매빈도 규칙
* 라이프사이클 규칙
* 로지스틱 회귀
* 트리 기반 모델

복잡한 모델이 단순 규칙보다 나은지 확인한다.

## 21. 데이터 분할

무작위 분할을 기본으로 사용하지 않는다.

시간 순서에 따라 나눈다.

```text
Train
→ Validation
→ Test
```

Feature 기간과 Label 기간을 분리한다.

고객의 미래 정보가 과거 Feature에 포함되지 않도록 테스트한다.

## 22. 모델 평가 지표

* ROC-AUC
* PR-AUC
* Log Loss
* Brier Score
* Calibration
* Precision@5%
* Precision@10%
* Precision@20%
* Recall@5%
* Recall@10%
* Recall@20%
* Lift@5%
* Lift@10%
* Lift@20%

CRM 의사결정에서는 AUC보다 Precision·Recall·Lift@K를 우선 설명한다.

---

# Phase 6. CRM 타기팅 시뮬레이션

## 23. 비교 정책

* 무작위 타기팅
* 최근성 타기팅
* 라이프사이클 규칙 타기팅
* 모델 기반 타기팅

접촉 가능 비율:

* 5%
* 10%
* 20%
* 30%

각 비율에서 다음을 계산한다.

* 선정 고객 수
* 실제 구매 비활성 고객 수
* 실제 향후 구매 고객 수
* Precision
* Recall
* Lift
* 단순 규칙 대비 개선
* 동일 포착률에 필요한 접촉 고객 수
* 불필요한 접촉 감소율

## 24. 성과 표현

다음 형태로 결과를 작성한다.

```text
전체 고객의 상위 10%만 선택했을 때,
단순 최근성 기준보다 실제 구매 비활성 고객 포착률을 높였다.
```

다음 형태로 작성하지 않는다.

```text
CRM 캠페인을 통해 이탈률을 개선했다.
```

---

# Phase 7. Streamlit 대시보드

## 25. 대시보드 사용자

가상의 사용자는 다음과 같다.

```text
이커머스 CRM 담당자
```

대시보드의 목적은 시각화가 아니라 다음 의사결정을 지원하는 것이다.

* 어떤 고객군을 우선 관리할 것인가
* 접촉 가능 인원이 제한됐을 때 누구를 선택할 것인가
* 재활성화와 구매 유도 대상은 어떻게 다른가
* 현재 고객 라이프사이클은 어떻게 구성돼 있는가

## 26. 대시보드 페이지

### Page 1. CRM Overview

* 활동 고객
* 구매 고객
* 반복구매 고객
* 장바구니 고객
* 구매 비활성 위험 고객
* 향후 구매 가능 고객
* 핵심 퍼널

### Page 2. Funnel

* 탐색
* 장바구니 추가
* 제거
* 구매
* 고객군별 퍼널
* 카테고리별 퍼널
* 기간별 퍼널

### Page 3. Cohort & Retention

* 첫 관측 구매 코호트
* 7일 재구매
* 14일 재구매
* 28일 재구매
* 주간 구매자 리텐션
* 구매주기

### Page 4. Lifecycle

* 라이프사이클 분포
* 상태 이동
* 구매 감소 고객
* 비활성 고객
* 복귀 고객

### Page 5. Segment Explorer

* 세그먼트별 고객 수
* 행동 특성
* 구매율
* 구매주기
* 추천 CRM 목적
* 추천 액션

### Page 6. Targeting Simulator

사용자가 접촉 비율을 선택한다.

* 상위 5%
* 상위 10%
* 상위 20%
* 상위 30%

출력:

* 대상 고객 수
* 위험 고객 포착 수
* 향후 구매 고객 포착 수
* Lift
* 규칙 기반 대비 개선
* 세그먼트 구성

### Page 7. AI CRM Report

* 선택 기간
* 선택 세그먼트
* 핵심 KPI
* 위험 신호
* 권장 액션
* 검증이 필요한 가설
* 운영 적용 시 A/B 테스트안

---

# Phase 8. LLM CRM 리포트

## 27. LLM 역할

LLM은 수치를 계산하지 않는다.

다음 파이프라인만 허용한다.

```text
DuckDB SQL
→ Python 검증
→ Pydantic JSON
→ LLM
→ CRM 리포트
```

## 28. LLM 출력 구분

LLM 보고서는 반드시 다음 네 영역으로 구분한다.

### Data Facts

SQL에서 확인된 사실

### Model Predictions

모델의 예측 결과

### Recommended Actions

LLM이 제안한 CRM 액션

### Testable Hypotheses

실제 캠페인 실험으로 검증해야 하는 가설

## 29. LLM 금지사항

* 입력에 없는 숫자 생성
* 매출 개선 수치 생성
* 실제 캠페인 성과처럼 표현
* 상품명 또는 검색어를 임의로 해석
* 인과효과 단정
* 근거 없는 고객 심리 추정
* 특정 고객을 부정적으로 단정

## 30. LLM 품질 테스트

다음을 테스트한다.

* 입력 숫자와 출력 숫자 일치
* 기간 정보 누락 여부
* 사실과 제안 구분
* 금지된 성과 표현
* 동일 입력에 대한 일관성
* 빈 데이터 처리
* 비정상 수치 처리
* 모델 결과가 없는 경우의 처리

---

# 31. 테스트

pytest와 SQL 테스트를 작성한다.

## 데이터 테스트

* 고객 ID 결측
* 타임스탬프 결측
* PK 중복
* 음수 또는 비정상 값
* 이벤트별 행 수 일치
* 상품 매칭률
* 날짜 범위
* 미래 데이터 누수
* Snapshot Feature·Label 분리

## 분석 테스트

* 퍼널 단계별 고객 수가 이전 단계보다 커지는 경우 검사
* 코호트 리텐션 값이 0~1 범위를 벗어나지 않음
* 고객별 첫 관측 구매일이 최소 구매일과 일치
* 구매간격이 음수가 아님
* 타기팅 비율과 선정 고객 수가 일치

## LLM 테스트

* 숫자 환각 금지
* 사실·예측·제안 구분
* 입력에 없는 KPI 생성 금지
* 성과 과장 표현 금지

---

# 32. 프로젝트 디렉터리

```text
crm-targeting-intelligence/
├── CLAUDE.md
├── README.md
├── LICENSE
├── pyproject.toml
├── requirements.txt
├── .gitignore
├── .env.example
│
├── config/
│   ├── settings.yaml
│   ├── paths.yaml
│   └── model.yaml
│
├── data/
│   ├── raw/
│   ├── external/
│   ├── interim/
│   ├── processed/
│   └── dashboard/
│
├── sql/
│   ├── staging/
│   ├── intermediate/
│   ├── marts/
│   ├── features/
│   ├── labels/
│   └── tests/
│
├── src/
│   ├── data/
│   ├── features/
│   ├── analysis/
│   ├── models/
│   ├── targeting/
│   ├── dashboard/
│   ├── llm/
│   └── utils/
│
├── app/
│   ├── Home.py
│   └── pages/
│
├── tests/
│   ├── unit/
│   ├── integration/
│   └── data_quality/
│
├── reports/
│   ├── figures/
│   ├── tables/
│   └── model_cards/
│
├── scripts/
│   ├── download_data.py
│   ├── validate_data.py
│   ├── build_database.py
│   ├── run_analysis.py
│   ├── train_models.py
│   ├── build_dashboard_data.py
│   └── generate_crm_report.py
│
└── docs/
    ├── data_dictionary.md
    ├── erd.md
    ├── methodology.md
    ├── limitations.md
    └── deployment.md
```

---

# 33. GitHub 공개 원칙

## 공개 가능

* Python 코드
* SQL 코드
* 테스트
* ERD
* 데이터 사전
* 분석 방법론
* 집계 결과
* 모델 평가 결과
* 합성 샘플 데이터
* 대시보드 코드
* LLM 프롬프트
* 리포트 예시
* 프로젝트 한계

## 공개 금지

* 원본 대용량 데이터
* 고객별 전체 행동 로그
* 원본 고객 ID
* 원본 DuckDB 파일
* API 키
* `.env`
* 원본에서 추출한 대규모 샘플
* 원본 복원이 가능한 데이터

`.gitignore`에 반드시 포함한다.

```text
data/raw/
data/external/
*.duckdb
*.db
.env
.env.*
models/
artifacts/
```

---

# 34. README 구성

README 첫 화면은 데이터셋 소개가 아니라 비즈니스 문제로 시작한다.

잘못된 시작:

```text
Synerise RecSys Challenge 데이터를 사용해 모델을 학습했다.
```

권장 시작:

```text
모든 고객에게 동일한 CRM 액션을 적용하는 대신,
고객의 탐색·장바구니·구매 행동을 이용해
재활성화 대상과 구매 유도 대상을 우선 선정했다.
```

README 구성:

1. 문제 정의
2. 데이터 출처
3. 데이터 한계
4. 전체 아키텍처
5. SQL 데이터마트
6. 고객분석
7. 모델링
8. 타기팅 시뮬레이션
9. 대시보드
10. LLM 리포트
11. 핵심 결과
12. 한계
13. 실제 운영 시 실험 설계
14. 실행 방법

---

# 35. Claude Code 작업 규칙

1. 사용자와 합의되지 않은 기준을 임의로 확정하지 않는다.
2. 데이터 확인 전 라이프사이클 기준을 만들지 않는다.
3. 데이터 확인 전 14일 이탈 기준을 고정하지 않는다.
4. 데이터 확인 전 모델 종류를 확정하지 않는다.
5. 단순 기준선을 먼저 만든다.
6. 모델보다 SQL 데이터마트를 우선한다.
7. 모든 지표에 명확한 분모와 분자를 기록한다.
8. 모든 테이블에 Grain을 문서화한다.
9. 미래 정보 누수 방지 테스트를 작성한다.
10. 원본 데이터는 GitHub에 올리지 않는다.
11. 실제 매출과 이탈률 개선을 주장하지 않는다.
12. LLM이 숫자를 계산하도록 하지 않는다.
13. Notebook보다 실행 가능한 Python 모듈과 SQL 파일을 우선한다.
14. 코드 실행 후 생성된 결과를 실제로 검증한다.
15. 오류를 숨기거나 임의로 값을 대체하지 않는다.
16. 결측값은 의미를 확인한 후 처리한다.
17. 모든 주요 결정은 `docs/methodology.md`에 기록한다.
18. 모든 한계는 `docs/limitations.md`에 기록한다.
19. Phase가 끝날 때마다 결과와 다음 선택지를 보고한다.
20. 현재 지시에서는 Phase 1까지만 수행한다.

---

# 36. 현재 Claude Code에 내릴 첫 명령

```text
CLAUDE.md를 먼저 읽어라.

현재는 Phase 1만 수행한다.

공식 Synerise RecSys Challenge 2025 원본 데이터의 확보 방법과
이용 조건을 확인하고, 데이터를 다운로드한 뒤 다음을 검증하라.

1. 모든 파일의 행·열 수와 스키마
2. 이벤트별 최소·최대 타임스탬프
3. 최근 3년 기준 충족 여부
4. 고객 ID의 이벤트 간 연결성
5. 상품 테이블 매칭률
6. 중복·결측·비정상 이벤트
7. 고객별 관찰기간
8. 구매 고객과 반복구매 고객 규모
9. 구매간격 분포
10. 장바구니와 구매의 시간 연결 가능성
11. 퍼널·코호트·리텐션·라이프사이클 분석 가능 여부
12. 구매 비활성·구매성향 모델링 가능 여부
13. 대시보드와 LLM 리포트 구현 가능 여부

결과는 reports/ 아래의 Phase 1 산출물로 저장하라.

Phase 1 결과를 요약하고,
가능한 프로젝트 주제 2~3개와 장단점을 제시하라.

사용자 검토 없이 Phase 2로 넘어가지 마라.
```
